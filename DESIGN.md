# NANAiRO 表示サポート — デザインガイド

更新日：2026-09-18。対象：LP。実装の正本は `src/landing.css`、構造と文言は `index.html`。

## 参照サイトの検証

参照：[NANAiRO Mag.](https://nanairo.design/)。ブラウザでトップ・記事一覧を観察し、DOMの計算済みスタイルおよび配信CSSを確認した。以下は観察結果と、このLPの採用値を区別して記録している。参照サイトの将来の変更には自動追従しない。

| 要素 | 参照で確認した特徴 | LPで採用するルール |
| --- | --- | --- |
| 背景 | 白地。ヒーロー周辺に淡いピンク・青緑のにじみ | 本文は白。色のにじみはヒーローだけ |
| ヘッダー | 濃色のお知らせ帯、横ロゴ、余白の広いナビゲーション | 40px前後の帯、160pxの横ロゴ、46px以上のナビ |
| ヒーロー | 大きな角丸ビジュアルに説明枠を左から重ねる | 左のコピー枠を右ビジュアルへ72px重ねる |
| ボタン | 実測 `rgb(51,132,135)` = `#338487`、半径9999px | 同系の濃い `#397579` とピル型。白文字の読みやすさを優先 |
| 文字 | bodyの先頭フォント指定はPoppins。日本語は環境フォントへフォールバック | 日本語システムフォント中心。外部フォントの通信は追加しない |
| 記事見出し | 確認した記事h3は20px・600、濃いグレー | カードh3は20〜24px、本文は13〜15px |
| カード | 白、細い枠線、角丸、影は控えめ。カテゴリは小さなピル | 通常カードには影を付けず、1px枠・24px角丸 |
| 見出し | 日本語見出しの下に英語補助ラベル | 中央見出し → 英語 → 説明文の順 |

記事写真・作品画像は転用せず、表示サポートのプレビューと緑系のCSS図形を使用する。LPの目的は「Webを見やすくし、より多くの人へ情報を届けること」。街中アートの説明は追加しない。

## デザイントークン

| CSS変数 | 値 | 用途 |
| --- | --- | --- |
| `--ink` | `#222936` | 本文・見出し・お知らせ帯 |
| `--muted` | `#606975` | 説明・補助テキスト |
| `--teal` | `#397579` | 主要ボタン・アクセント |
| `--teal-hover` | `#285c60` | ボタンのホバー |
| `--paper` | `#ffffff` | 基本背景 |
| `--soft` | `#f5f7f7` | 小さなタグ・補助面 |
| `--line` | `#e8ebeb` | 装飾的な区切り・カード枠 |
| `--radius` | `24px` | カード・大きな面 |

ピルは999px、コード枠は16px。罫線だけで操作部品を識別させず、ラベル・背景・フォーカス輪郭を併用する。色のみで状態を伝えない。

## レイアウトと文字組み

- 最大幅1120px。通常の左右余白48px、900px以下では24px。
- セクション上下80px、700px以下48px。カード間24pxを基本にする。
- H1は32〜48px、行高1.5。セクション見出し26〜36px、行高1.6。
- 本文は行高1.8〜2。日本語の字間を過度に詰めない。
- 導入方法と機能はPCで3列、700px以下で1列。
- ヒーローはPCで重ねる2列、モバイルではコピー→ビジュアルの縦並び。
- モバイルのヘッダーはロゴとダウンロード導線を表示する。
- 最下部のデモエリアは常に1カラム、見出し・説明・ボタンを中央揃え。
- コードは枠内で横スクロール。グリッドの子は必要に応じて `min-width: 0` を指定する。

## コンポーネントの使い方

`section-intro` はセクション見出しに使用する。`eyebrow` は英語の補助ラベルで、必須情報は日本語にも記載する。手書き系ラベルとツールフッターの `Powered by` には、プロジェクト内へ同梱したGoogle Fontsの「Reenie Beanie」を使用し、OSに依存せず同じ表情を保つ。英語ラベルは28px・400・`#6b7280`・標準字間・Title Caseを基本とし、日本語見出しから8px空ける。`Powered by` は補助表記のため18pxとする。

`primary` は主要操作、`secondary` は関連情報への移動、`download-button` は実ファイルの取得に使う。主要ボタンは単色の青緑で、光沢・強い影・多色グラデーションを追加しない。最小高さ50px、デモCTAは56px。

`download-card` には方式名、対象者、初心者向け説明、補助タグ、ダウンロードを順に置く。`install-card` には貼り付け例と3段階の手順を置く。ダウンロードリンクは `./downloads/…` とし、GitHub Pagesのサブディレクトリでも動作させる。

ヒーローの半透明プレビューは装飾として `aria-hidden` にする。本物の操作は右端の表示サポートまたはデモCTAから行う。

## ロゴとウィジェット

横ロゴは `public/assets/nanairo-logo-horizontal.png`、マークは `public/assets/nanairo-logo-mark.png` を使用する。縦横比を保ち、文字でロゴを代用しない。

LPと埋め込みウィジェットに同じデザインシステムを適用する。ウィジェットは `src/accessibility-widget.ts` のShadow DOM内で独立させ、LPのCSSで内部を上書きしない。配布先に依存しないよう同じトークン値を内部に定義し、変更時はLPと照合する。

- 基本色：青緑 `#397579`、ホバー `#285c60`、本文 `#222936`、補助文 `#606975`、枠線 `#e8ebeb`、淡い背景 `#f5f7f7`。
- パネルは不透明な白、カードは24px角丸と細い枠線。強い光沢・背景ぼかし・装飾グラデーションを使わない。影はページとの境界を示すドロワーと追従タブに限定する（小さなスイッチや色見本の影を除く）。
- 操作アイコンは青緑、選択状態は単色の青緑と白。カラーモードの色見本だけは機能を伝えるため多色を許容する。
- 補助文は12px、行高1.6前後。省略せず折り返す。閉じる・文字サイズ操作・リセット・言語選択は44px以上の高さを確保する。
- 右端の2段「表示／サポート」、0.48秒の連動するドロワー開閉、各表示機能、末尾の言語選択を維持する。パネル見出しは「表示サポートツール」とし、その下に外部リンクアイコン付きの「ウェブアクセシビリティ（無料入手）」タグを置く。タグとフッターの `Powered by`＋ロゴは、いずれも `https://nanairo.design/accessibility-tools` へ新しいタブでリンクする。動きを減らすOS設定を尊重する。
- 枠線だけで選択を表さず、背景色・スイッチ位置と既存のARIA状態を併用する。キーボードフォーカスはカード内部でも切れないようにする。

## 継続改修の手順

1. このガイドを読み、既存トークンとクラスを再利用する。
2. LPの変更は `index.html` と `src/landing.css` に行う。インラインCSSや後付けの上書き集を増やさない。
3. `npm run build:site` でPages版をビルドする。ライブラリ版の `npm run build` とは別。
4. PCと390px前後のスマートフォン幅で、見出し・カード・コード枠・CTAに横はみ出しがないことを確認する。
5. Tabキーのフォーカス、デモを開く操作、Escapeで閉じる操作、3形式のダウンロードリンクを確認する。
6. `prefers-reduced-motion` を尊重し、動きは短いホバー反応に留める。
7. `main`へ反映後、GitHub Actionsの成功と公開ページの実表示を確認する。

LPのみの変更ではウィジェット配布ZIPを再生成する必要はない。ウィジェットを変更した場合はnpm・CDN・WordPressを同じソースで再生成する。
