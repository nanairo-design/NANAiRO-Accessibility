# NANAiRO Accessibility 機能監査

監査対象: デジタル庁「ウェブアクセシビリティ導入ガイドブック」（2025年10月16日版）第3章

判定は、NANAiRO Accessibilityを導入しただけで対象サイトの要件を満たせるかという観点で行っています。補助ツール自身の操作性が良好でも、サイト本体のHTML、コンテンツ、動画、フォーム等を自動修正できない項目は「未対応」または「部分対応」です。

## 3.1 重大（非干渉）

| 項目 | 判定 | 現状と不足 |
| --- | --- | --- |
| 自動再生はさせない | 部分対応 | 「メディアを停止・ミュート」で既存・後追加の動画と音声を停止できます。ただし利用者が有効にする前の自動再生を防がず、3秒以内かの判定も行いません。 |
| 袋小路に陥らせない | 未対応 | ツール自身はネイティブボタン、Escapeでの終了、フォーカス復帰を備えていますが、導入先のモーダルやプレイヤーのキーボードトラップは検出・修正しません。 |
| 光の点滅は危険 | 未対応 | 「動きを減らす」はCSSアニメーション等を抑えますが、動画、Canvas、GIF等の点滅回数を検出・停止できません。 |
| 自動でコンテンツを切り替えない | 未対応 | 導入先のカルーセル、ティッカー、自動更新コンテンツへ一時停止・停止・非表示操作を追加しません。 |

## 3.2 必須

| 項目 | 判定 | 現状と不足 |
| --- | --- | --- |
| 画像に代替テキストを付与する | 未対応 | 画像の意味を判定して適切な`alt`を作成・設定する機能はありません。 |
| すべての機能をキーボード操作可能にする | 部分対応 | 表示サポート自体はキーボード操作、フォーカス表示、開閉時のフォーカス移動に対応しています。導入先サイト全体の操作性は修正しません。 |
| 操作に制限時間を設けない | 未対応 | セッションや入力期限の検出、事前通知、延長、解除機能はありません。 |
| 色・形・位置など単一の表現だけで伝えない | 未対応 | カラーモードはありますが、導入先コンテンツが色や位置だけで意味を伝えているかを判定・補完しません。 |
| 読み上げ順序を意味の通る順序にする | 未対応 | 読み上げ機能はDOM順で本文を読みますが、DOM順やフォーカス順そのものを修正しません。 |
| 見出しでセクションの内容を表現する | 未対応 | 見出しレベル、空見出し、見出し名の妥当性を検査・修正しません。 |
| 十分なコントラスト比を保つ | 部分対応 | ダーク、ライト、高コントラスト等を提供しますが、任意サイトの全要素、画像内文字、Canvas等で基準値を保証できません。 |
| 200%拡大でも情報を読み取れる | 不足あり | 現在のツール内文字拡大は最大175%です。導入先レイアウトの重なり・見切れも自動修正しません。 |
| 文字コード・文字・フォントに注意する | 部分対応 | 読みやすいフォントへ切り替えられますが、文書の文字コード、誤った文字、記号の読み上げを検査・修正しません。 |
| 適切なページタイトルを付ける | 未対応 | `title`の内容、重複、H1との整合性を検査・修正しません。 |
| リンクを適切に表現する | 部分対応 | リンク強調はできますが、リンク文言、リンク先形式、別ウィンドウ表示の説明は補完しません。 |
| ナビゲーションと同じ機能の表示を一貫させる | 未対応 | 導入先のページ間で順序、名称、アイコン、説明が一貫しているかを検査・修正しません。 |

## 優先して改善できる製品機能

1. 文字拡大の上限を175%から200%へ変更し、主要レイアウトでリフローを検証する。
2. 導入先ページの簡易監査機能を追加する。候補は`alt`不足、見出し構造、ページタイトル、リンク名、フォームラベル、フォーカス可視性です。
3. 動画・音声の自動再生を初期状態から抑止できるオプションを追加する。
4. カルーセルや点滅コンテンツは汎用的な自動修正が難しいため、検出結果と改善方法をサイト管理者へ提示する。
5. 「本ツールの導入だけではJIS X 8341-3／WCAGへの適合を保証しない」という説明を配布資料と管理画面で継続して明示する。
