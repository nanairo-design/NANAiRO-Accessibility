# NANAiRO Accessibility 機能監査

監査対象: デジタル庁「ウェブアクセシビリティ導入ガイドブック」（2025年10月16日版）第3章

判定は、NANAiRO Accessibilityを導入しただけで対象サイトの要件を満たせるかという観点で行っています。補助ツール自身の操作性が良好でも、サイト本体のHTML、コンテンツ、動画、フォーム等を自動修正できない項目は「未対応」または「部分対応」です。

## 前提：ガイドブックは本製品の表示調整機能を「非推奨」としています

同ガイドブックの「3.4 導入に慎重な検討が必要（非推奨）」に、次の2項目が挙げられています。

- 「アクセシビリティ・オーバーレイなどのプラグインは支援技術の機能と重複させない」
- 「文字サイズの変更、読み上げプラグインの利用は非推奨」（図3.23「文字サイズ変更、配色変更、読み上げなどの、ウェブページに埋め込むプラグインは非推奨」）

理由として、支援技術を必要とする利用者はすでにOS・ブラウザ側の機能を使っているため過剰対応になること、サイト単位の機能は他サイトで使えず効果が限定的であることが示されています。また「2.1 よくある誤解」では、文字サイズ拡大ボタンやカラーテーマ変更ボタンを設置してもアクセシビリティは確保できず、「補助機能を付けるよりも前に、利用者がデバイスの設定機能で変更しようとしている文字サイズや色変更に対応できるような実装」を優先すべきと述べられています。

したがって**必須項目はサイト本体の実装とコンテンツで達成するものであり、本ツールに機能を追加して達成できる項目はありません**。本ツールの位置づけは次の2つに限られます。

1. 利用者が自分の環境で表示を調整する手段の提供（ガイドブックが非推奨とする範囲。OS・ブラウザ側の設定に対応することが本来の優先事項です）
2. サイト管理者向けの問題候補の検出（支援技術の機能と重複せず、上記の非推奨には該当しません）

同ガイドブックは「チェックツールで見つけられる問題は、ウェブアクセシビリティの問題の2割から3割程度」とも述べています。簡易チェックの各項目には目視確認が必要な旨を常時併記しています。

## 3.1 重大（非干渉）

| 項目 | 判定 | 現状と不足 |
| --- | --- | --- |
| 自動再生はさせない | 部分対応・検出支援 | 「メディアを停止・ミュート」で既存・後追加の動画と音声を停止できます。簡易チェックでミュートされていない`autoplay`指定を検出します。利用者が有効にする前の自動再生は防げず、動画内音声と3秒以内かの判定は目視確認が必要です。 |
| 袋小路に陥らせない | 未対応・目視確認 | ツール自身はネイティブボタン、Escapeでの終了、フォーカス復帰を備えています。導入先のキーボードトラップは自動判定できないため、簡易チェックでは目視確認項目として提示します。 |
| 光の点滅は危険 | 未対応・検出支援 | 「動きを減らす」はCSSアニメーション等を抑えます。簡易チェックで1秒に3回を超える無限アニメーションと`marquee`を検出します。動画、Canvas、GIFの点滅は検出できません。 |
| 自動でコンテンツを切り替えない | 未対応・検出支援 | 簡易チェックで無限に動き続ける要素と`marquee`を検出します。導入先のカルーセル等へ一時停止・停止・非表示操作を追加はしません。 |

## 3.2 必須

| 項目 | 判定 | 現状と不足 |
| --- | --- | --- |
| 画像に代替テキストを付与する | 未対応・検出支援 | `alt`属性がない画像を簡易チェックで検出します。画像の意味を判断した代替テキストの作成・設定は、サイト管理者による確認が必要です。 |
| すべての機能をキーボード操作可能にする | 部分対応 | 表示サポート自体はキーボード操作、フォーカス表示、開閉時のフォーカス移動に対応しています。導入先サイト全体の操作性は修正しません。 |
| 操作に制限時間を設けない | 未対応・目視確認 | 簡易チェックで確認項目として提示します。セッションや入力期限の事前通知、延長、解除は導入先での実装が必要です。 |
| 色・形・位置など単一の表現だけで伝えない | 未対応・目視確認 | 簡易チェックで確認項目として提示します。意味の判断と、テキストや記号の追加はサイト管理者が行います。 |
| 読み上げ順序を意味の通る順序にする | 未対応・検出支援 | 正の`tabindex`やCSSの`order`を問題候補として検出します。意味の通るDOM順・フォーカス順への修正は行いません。 |
| 見出しでセクションの内容を表現する | 未対応・検出支援 | 空見出し、H1不足、見出しレベルの飛びを簡易チェックで検出します。見出し文言や構造は自動修正しません。 |
| 十分なコントラスト比を保つ | 部分対応・検出支援 | ダーク、ライト、高コントラスト等を提供します。簡易チェックで基準値未満の文字を検出します（測定は本ツールの配色変更を一時停止して行います）。画像・グラデーション上の文字は測定できず目視確認が必要です。 |
| 200%拡大でも情報を読み取れる | 部分対応 | ツール内文字拡大は200%まで選択できます。導入先レイアウトの重なり・見切れ・横スクロールは実ページでの確認と修正が必要です。 |
| 文字コード・文字・フォントに注意する | 部分対応・検出支援 | 読みやすいフォントへ切り替えられます。簡易チェックでUTF-8以外の文字コードと極端に小さい文字を検出します。誤った文字や記号の読み上げは目視確認が必要です。 |
| 適切なページタイトルを付ける | 未対応・検出支援 | 空または一般的すぎる`title`を問題候補として検出します。ページ間の重複やH1との整合性はサイト全体での確認が必要です。 |
| リンクを適切に表現する | 部分対応 | リンク強調はできますが、リンク文言、リンク先形式、別ウィンドウ表示の説明は補完しません。 |
| ナビゲーションと同じ機能の表示を一貫させる | 未対応・検出支援 | 名前のない操作要素を問題候補として検出し、ページ間の順序・名称・アイコン・説明は目視確認項目として提示します。 |

## 優先して改善できる製品機能

1. 【対応済み】文字拡大の上限を200%へ変更し、スマートフォン幅でも表示サポートを全幅で操作できるようにする。
2. 【対応済み】簡易アクセシビリティチェックを追加。3.1の重大4項目と3.2の必須12項目を、重大／必須に分けて表示する。
3. 【対応済み】各項目に目視確認が必要な旨を常時併記する（自動チェックで見つかるのは問題の2〜3割）。
4. 【対応済み】検査はウィジェット自身の配色・文字サイズ変更を一時停止して実行する。有効なまま測るとサイト本来の値ではなく本ツールが上書きした値を測ってしまう。
5. 動画・音声の自動再生を初期状態から抑止できるオプションを追加する。
6. カルーセルや点滅コンテンツは汎用的な自動修正が難しいため、検出結果と改善方法をサイト管理者へ提示する。
7. 「本ツールの導入だけではJIS X 8341-3／WCAGへの適合を保証しない」という説明を配布資料と管理画面で継続して明示する。

## 簡易チェックの限界（実装上の既知の制約）

- コントラストは`rgb()`／`rgba()`で表現できる色のみ測定します。背景画像・グラデーション上の文字と、解析できない色指定の文字は「測定不能」として目視確認項目に回します。
- 色・形だけで情報を伝えていないかは、DOMから判定できないため常に目視確認項目として提示します。
- キーボードトラップは自動判定できないため常に目視確認項目です。
- ページ間の比較が必要な項目（ページタイトルの重複、ナビゲーションの一貫性、同一機能のラベル）は1ページの検査では完結しません。
