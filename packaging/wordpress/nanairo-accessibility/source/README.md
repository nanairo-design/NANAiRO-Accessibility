# Build source

The human-readable TypeScript source for the bundled widget is included in this directory.

```bash
npm install
npm run build
```

The WordPress plugin loads `assets/js/nanairo-accessibility.iife.js` from the generated `dist` directory.
