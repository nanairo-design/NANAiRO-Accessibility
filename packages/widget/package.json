{
  "name": "@nanairo/widget",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/widget-core-1.0.0.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsup src/index.ts --format esm --target es2020 --dts --out-dir dist && node ../../scripts/rename-widget.mjs",
    "dev": "tsup src/index.ts --format esm --target es2020 --dts --watch --out-dir dist",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@nanairo/core": "file:../core"
  },
  "devDependencies": {
    "tsup": "^8.2.4",
    "typescript": "^5.6.2"
  }
}
