{
  "name": "@nanairo/loader",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/loader.js",
  "files": ["dist"],
  "scripts": {
    "build": "tsup src/index.ts --format iife --global-name NANAiROLoader --target es2020 --minify --out-dir dist && node ../../scripts/rename-loader.mjs",
    "dev": "tsup src/index.ts --format iife --global-name NANAiROLoader --target es2020 --watch --out-dir dist",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@nanairo/core": "file:../core"
  },
  "devDependencies": {
    "tsup": "^8.2.4",
    "typescript": "^5.6.2"
  }
}
