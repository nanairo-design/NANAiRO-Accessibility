# Dashboard (MVP scope)

このディレクトリは NANAiRO Web SDK の SaaS 管理画面向けの領域です。

MVP では以下を実装対象とします。

- ドメイン登録
- UIテーマ編集
- 機能 ON/OFF
- API キー発行
- 利用統計の基本表示

本格実装時は Next.js で `apps/dashboard` に構築し、`apps/api` の設定APIと連携します。
